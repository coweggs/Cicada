<Window
        x:Class="Cicada.Flyout"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cicada"
        mc:Ignorable="d"
        AllowsTransparency="True" Background="Transparent"
        WindowStyle="None" ShowInTaskbar="False"
        Topmost="True" ResizeMode="NoResize"
        Width="195" Height="50">

    <Border Background="#242424"
            Width="195" Height="50"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="#555">
        <StackPanel Orientation="Horizontal" Margin="12">
            <Grid>
                <Image x:Name="AppIconImage"
                        Source="/cicada.ico" Width="24" Height="24"
                        Margin="0 0 10 0" Opacity="1"/>
                <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE74F;"
                       Foreground="White" Margin="5" x:Name="MuteIcon"
                       FontSize="16" Opacity="0"/>
            </Grid>
            <Border Width="108">
                <Border Width="108" Height="5" CornerRadius="3"
                        Background="#9f9f9f">
                    <Border x:Name="SliderBar"
                            Width="45.76" Height="5" CornerRadius="3"
                            HorizontalAlignment="Left"
                        Background="#4cc2ff"/>
                </Border>
            </Border>
            <TextBlock x:Name="VolumeText"
                        FontFamily="Segoe UI" TextAlignment="Center"
                        VerticalAlignment="Center" FontSize="14"
                        Margin="5 0 0 0"
                        Text="32" Width="22" Foreground="White"/>
        </StackPanel>
    </Border>
</Window>
